name: Package and Upload Builds

run-name: Publish ${{ inputs.official_release && 'official release' || 'snapshot' }} for ${{ github.event.workflow_run.head_sha || github.sha }}

on:
  workflow_run:
    workflows:
      - Build all
    types:
      - completed
  workflow_dispatch:
    inputs:
      official_release:
        description: Publish a stable Major.Minor.Patch release
        type: boolean
        required: true
        default: false

permissions:
  contents: read

env:
  VULKAN_SDK: ''
  PUBLISH_SHA: ${{ github.event.workflow_run.head_sha || github.sha }}
  OFFICIAL_RELEASE: ${{ github.event_name == 'workflow_dispatch' && inputs.official_release || false }}

jobs:
  build-package:
    name: Build ${{ matrix.platform }} release package
    # Manual runs bypass CI validation; automatic runs require a successful push build on the release branch.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.head_branch == (vars.SNAPSHOT_RELEASE_BRANCH || 'master')
      )
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: Windows
            runner: windows-2025-vs2026
          - platform: Linux
            runner: ubuntu-24.04
    concurrency:
      group: package-releases-${{ matrix.platform }}-${{ inputs.official_release && (github.event.workflow_run.head_sha || github.sha) || 'snapshot' }}
      cancel-in-progress: ${{ ! inputs.official_release }}

    steps:
      - name: Checkout publish commit
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ env.PUBLISH_SHA }}
          # Keep full main-repository history so publish.ps1 can extract the Git revision count.
          fetch-depth: 0
          submodules: false
          persist-credentials: false

      - name: Checkout shallow submodules
        run: git submodule update --init --recursive --depth 1

      - name: Install Linux toolchain and dependencies
        if: matrix.platform == 'Linux'
        run: |
          # Pin the installer so release toolchain setup changes only through review.
          curl --proto '=https' --tlsv1.2 -fsSLo llvm.sh https://raw.githubusercontent.com/opencollab/llvm-jenkins.debian.net/c5ecff7c7208c8b5225e1f6e6ae6f2539e1d31ea/llvm.sh
          sudo bash llvm.sh 22
          sudo apt-get install -y llvm-22 libvulkan-dev libgl1-mesa-dev libgtk-3-dev libwayland-dev libx11-dev p7zip-full wayland-protocols
          echo "CC=clang-22" >> "$GITHUB_ENV"
          echo "CXX=clang++-22" >> "$GITHUB_ENV"

      - name: Cache Vulkan SDK
        uses: actions/cache@v4
        with:
          path: publish/_deps/vulkan-sdk
          key: vulkan-${{ runner.os }}-${{ runner.arch }}-${{ github.job }}-${{ hashFiles('OIVLib/Renderers/OIVVKRenderer/VulkanSDK.cmake') }}

      - name: Build release binaries
        shell: pwsh
        run: ./publish.ps1 -OfficialBuild $true -OfficialRelease ([bool]::Parse($env:OFFICIAL_RELEASE)) -EnablePackage $false -CMakeDefinitions @{ lerc = "OFF" }

      - name: Bundle Linux libstdc++ runtime
        if: matrix.platform == 'Linux'
        run: |
          for library in libstdc++.so.6 libgcc_s.so.1; do
            source_path="$(clang++-22 -print-file-name="$library")"
            test "$source_path" != "$library"
            test -f "$source_path"
            cp --dereference "$source_path" "publish/bin/$library"
          done

      - name: Package release
        shell: pwsh
        run: ./publish.ps1 -OfficialBuild $true -OfficialRelease ([bool]::Parse($env:OFFICIAL_RELEASE)) -EnableConfigure $false -EnableBuild $false

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-package-${{ matrix.platform }}
          path: publish/**/OIV-*
          if-no-files-found: error
          retention-days: 7

  publish-release:
    name: Publish release
    needs: build-package
    environment:
      name: ${{ github.event_name == 'workflow_dispatch' && inputs.official_release && 'official-release' || 'snapshot-release' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      actions: read
      contents: write
    concurrency:
      group: publish-releases-${{ inputs.official_release && (github.event.workflow_run.head_sha || github.sha) || 'snapshot' }}
      cancel-in-progress: false
    env:
      RELEASE_REPOSITORY: ${{ github.repository }}

    steps:
      - name: Checkout release tooling
        uses: actions/checkout@v4
        with:
          ref: ${{ github.workflow_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Download package artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-package-*
          path: release-package
          merge-multiple: true

      - name: Select downloaded archives
        id: package
        shell: pwsh
        run: |
          $archivePattern = '^OIV-.+(?:\.[^.]+)+$'
          $symbolsPattern = '-Symbols(?:\.[^.]+)+$'
          $archives = @(Get-ChildItem -Path "release-package" -Recurse -File | Where-Object { $_.Name -match $archivePattern })
          $archiveGroups = [ordered]@{
              "Windows runtime" = @($archives | Where-Object { $_.Name -notmatch $symbolsPattern -and $_.Name -match '-Win32-[^-]+-[^-.]+\.' })
              "Linux runtime"   = @($archives | Where-Object { $_.Name -notmatch $symbolsPattern -and $_.Name -match '-linux-[^-]+-[^-.]+\.' })
              "symbols"         = @($archives | Where-Object { $_.Name -match $symbolsPattern })
          }
          foreach ($group in $archiveGroups.GetEnumerator()) {
              if ($group.Value.Count -ne 1) {
                  $archiveList = if ($group.Value.Count -eq 0) { "<none>" } else { ($group.Value.FullName -join "`n") }
                  throw "Expected exactly one $($group.Key) archive, found $($group.Value.Count):`n$archiveList"
              }
          }

          $windowsRuntimeArchive = $archiveGroups["Windows runtime"][0]
          $linuxRuntimeArchive = $archiveGroups["Linux runtime"][0]
          $symbolsArchive = $archiveGroups["symbols"][0]
          $releaseArchives = @($windowsRuntimeArchive, $linuxRuntimeArchive, $symbolsArchive)
          $officialRelease = [bool]::Parse($env:OFFICIAL_RELEASE)
          $titlePattern = if ($officialRelease) {
              '^(?<title>OIV-\d+\.\d+\.\d+)-.+(?:\.[^.]+)+$'
          } else {
              '^(?<title>OIV-\d+\.\d+\.\d+\.\d+)-[0-9a-f]+-.+(?:\.[^.]+)+$'
          }
          $releaseTitle = $null
          foreach ($archive in $releaseArchives) {
              if ($archive.Name -notmatch $titlePattern) {
                  throw "Archive name '$($archive.Name)' does not match the selected release mode."
              }
              $archiveTitle = $Matches.title
              if ($releaseTitle -and $archiveTitle -ne $releaseTitle) {
                  throw "Release archive versions do not match: $(($releaseArchives.Name | ForEach-Object { "'$_'" }) -join ', ')."
              }
              $releaseTitle = $archiveTitle
          }
          $releaseTag = if ($officialRelease) { $releaseTitle } else { "snapshot" }
          $prerelease = (-not $officialRelease).ToString().ToLowerInvariant()

          "windows_runtime_archive_path=$($windowsRuntimeArchive.FullName)" >> $env:GITHUB_OUTPUT
          "linux_runtime_archive_path=$($linuxRuntimeArchive.FullName)" >> $env:GITHUB_OUTPUT
          "symbols_archive_path=$($symbolsArchive.FullName)" >> $env:GITHUB_OUTPUT
          "release_title=$releaseTitle" >> $env:GITHUB_OUTPUT
          "release_tag=$releaseTag" >> $env:GITHUB_OUTPUT
          "prerelease=$prerelease" >> $env:GITHUB_OUTPUT
          "Selected Windows runtime archive $($windowsRuntimeArchive.FullName)"
          "Selected Linux runtime archive $($linuxRuntimeArchive.FullName)"
          "Selected symbols archive $($symbolsArchive.FullName)"
          "Release title $releaseTitle"

      - name: Publish release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.package.outputs.release_tag }}
          RELEASE_TARGET: ${{ env.PUBLISH_SHA }}
          RELEASE_TITLE: ${{ steps.package.outputs.release_title }}
          PRERELEASE: ${{ steps.package.outputs.prerelease }}
          WINDOWS_RUNTIME_ARCHIVE_PATH: ${{ steps.package.outputs.windows_runtime_archive_path }}
          LINUX_RUNTIME_ARCHIVE_PATH: ${{ steps.package.outputs.linux_runtime_archive_path }}
          SYMBOLS_ARCHIVE_PATH: ${{ steps.package.outputs.symbols_archive_path }}
        run: >
          ./.github/scripts/Publish-GitHubRelease.ps1
          -Repository $env:RELEASE_REPOSITORY
          -Tag $env:RELEASE_TAG
          -TargetSha $env:RELEASE_TARGET
          -Title $env:RELEASE_TITLE
          -Prerelease ([bool]::Parse($env:PRERELEASE))
          -MutableTag ([bool]::Parse($env:PRERELEASE))
          -Archives @($env:WINDOWS_RUNTIME_ARCHIVE_PATH, $env:LINUX_RUNTIME_ARCHIVE_PATH, $env:SYMBOLS_ARCHIVE_PATH)
