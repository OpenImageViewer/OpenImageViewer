cmake_minimum_required(VERSION 3.8)

if ( ${IMCODEC_BUILD_CODEC_DDS})
    add_subdirectory(CodecDDS)
endif()
if ( ${IMCODEC_BUILD_CODEC_PSD})
    add_subdirectory(CodecPSD)
endif()
if ( ${IMCODEC_BUILD_CODEC_JPG})
    add_subdirectory(CodecJPG)
endif()
if ( ${IMCODEC_BUILD_CODEC_PNG})
    add_subdirectory(CodecPNG)
endif()
if ( ${IMCODEC_BUILD_CODEC_GIF})
    add_subdirectory(CodecGif)
endif()
if ( ${IMCODEC_BUILD_CODEC_TIFF})
    add_subdirectory(CodecTIFF)
endif()
if ( ${IMCODEC_BUILD_CODEC_WEBP})
    add_subdirectory(CodecWEBP)
endif()

if ( ${IMCODEC_BUILD_CODEC_FREEIMAGE})
    add_subdirectory(CodecFreeImage)
endif()